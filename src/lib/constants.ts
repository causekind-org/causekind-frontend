// Cross-surface product constants. Single source of truth for numbers that
// marketing copy quotes — keep in sync with the backend's matching engine
// (MatchingEngineService distance staging) when it changes.
export const MATCH_RADIUS_KM = 10;

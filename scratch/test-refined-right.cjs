const sharp = require('sharp');
const fs = require('fs');

// Let's test refined right garland that stops above the bell
function testRightGarland() {
  const code = fs.readFileSync('scratch/modular_garland.tsx', 'utf8');
  // We can see how clean it is
}

testRightGarland();
